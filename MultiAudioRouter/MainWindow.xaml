<Window x:Class="MultiAudioRouter.MainWindow"
        x:Name="RootWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MultiAudioRouter"
        mc:Ignorable="d"
        Title="MultiAudioRouter" Height="720" Width="500"
        Background="#121214" Foreground="#F3F4F6"
        WindowStartupLocation="CenterScreen" ResizeMode="CanMinimize"
        FontFamily="Segoe UI, Arial">

    <Window.Resources>
        <!-- Color Definitions -->
        <SolidColorBrush x:Key="BgPrimary" Color="#121214"/>
        <SolidColorBrush x:Key="BgSecondary" Color="#1E1E24"/>
        <SolidColorBrush x:Key="BorderColor" Color="#2D2D34"/>
        <SolidColorBrush x:Key="AccentColor" Color="#6366F1"/>
        <SolidColorBrush x:Key="AccentHoverColor" Color="#4F46E5"/>
        <SolidColorBrush x:Key="SuccessColor" Color="#10B981"/>
        <SolidColorBrush x:Key="SuccessHoverColor" Color="#059669"/>
        <SolidColorBrush x:Key="DangerColor" Color="#EF4444"/>
        <SolidColorBrush x:Key="DangerHoverColor" Color="#DC2626"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#F3F4F6"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#9CA3AF"/>

        <!-- Custom ScrollViewer for ListBox -->
        <Style x:Key="ModernScrollbarThumb" TargetType="Thumb">
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="rectangle" SnapsToDevicePixels="True" Background="#4B5563" CornerRadius="3" Height="{TemplateBinding Height}" Width="{TemplateBinding Width}"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="rectangle" Property="Background" Value="#6B7280"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernScrollBar" TargetType="ScrollBar">
            <Setter Property="Stylus.IsPressAndHoldEnabled" Value="false"/>
            <Setter Property="Stylus.IsFlicksEnabled" Value="false"/>
            <Setter Property="Width" Value="6"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid x:Name="Bg" SnapsToDevicePixels="true" Background="Transparent">
                            <Track x:Name="PART_Track" IsDirectionReversed="true" IsEnabled="{TemplateBinding IsEnabled}">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.LineUpCommand" Opacity="0"/>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.LineDownCommand" Opacity="0"/>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ModernScrollbarThumb}"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Custom CheckBox Template -->
        <Style x:Key="ModernCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <BulletDecorator Background="Transparent">
                            <BulletDecorator.Bullet>
                                <Border x:Name="Border" Width="20" Height="20" CornerRadius="6" Background="#2A2A35" BorderBrush="{StaticResource BorderColor}" BorderThickness="1.5">
                                    <Grid>
                                        <Path x:Name="CheckMark" Data="M 3,8 L 8,13 L 17,4" Stroke="#F3F4F6" StrokeThickness="2.5" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Visibility="Collapsed"/>
                                    </Grid>
                                </Border>
                            </BulletDecorator.Bullet>
                            <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                        </BulletDecorator>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="true">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource AccentColor}"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Button Base Style -->
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="42"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="8" BorderThickness="0" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Background" Value="#2A2A35"/>
                                <Setter TargetName="Border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- Custom Slider Styling -->
        <Style x:Key="ModernSlider" TargetType="Slider">
            <Setter Property="Background" Value="#2D2D34"/>
            <Setter Property="Foreground" Value="{StaticResource AccentColor}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto" MinHeight="{TemplateBinding MinHeight}"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Border x:Name="TrackBackground" Grid.Row="1" Background="{TemplateBinding Background}" Height="4" CornerRadius="2" VerticalAlignment="Center"/>
                            <Track x:Name="PART_Track" Grid.Row="1">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="Slider.DecreaseLarge" Background="{TemplateBinding Foreground}" BorderBrush="Transparent" Height="4">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="2"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="Slider.IncreaseLarge" Background="Transparent" BorderBrush="Transparent">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Background="Transparent"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb x:Name="Thumb" Width="12" Height="12">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Ellipse Fill="{StaticResource AccentColor}" Stroke="White" StrokeThickness="1" Width="12" Height="12"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Custom ComboBoxItem Styling -->
        <Style x:Key="ModernComboBoxItem" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#1E1E24"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Gd" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="Gd" Property="Background" Value="#312E81"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="true">
                                <Setter TargetName="Gd" Property="Background" Value="#6366F1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Custom ComboBox Styling -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#2A2A35"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="22"/>
            <Setter Property="Padding" Value="5,2"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource ModernComboBoxItem}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Grid.Column="2" Focusable="false" IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition />
                                                    <ColumnDefinition Width="20" />
                                                </Grid.ColumnDefinitions>
                                                <Path Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" Fill="#9CA3AF" Data="M 0 0 L 4 4 L 8 0 Z"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="true">
                                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Margin="8,3,23,3" VerticalAlignment="Center" HorizontalAlignment="Left" />
                            <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border Name="DropDownBorder" Background="#1E1E24" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" CornerRadius="4"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="Auto"/> <!-- Global Crossover Slider -->
            <RowDefinition Height="Auto"/> <!-- Control Panel -->
            <RowDefinition Height="*"/>    <!-- Device List -->
            <RowDefinition Height="Auto"/> <!-- Action Panel -->
            <RowDefinition Height="Auto"/> <!-- Activity Indicator / Stats -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="MultiAudioRouter" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                    <TextBlock Text="Route system audio to multiple outputs" FontSize="12" Foreground="{StaticResource TextSecondary}" Margin="0,2,0,0"/>
                </StackPanel>
                <!-- Status Dot -->
                <Border Grid.Column="1" x:Name="StatusDot" Width="10" Height="10" CornerRadius="5" Background="#4B5563" VerticalAlignment="Center" Margin="0,0,10,0" ToolTip="Inactive"/>
            </Grid>
            <Border Height="1" Background="{StaticResource BorderColor}" Margin="0,15,0,0"/>
        </StackPanel>

        <!-- Global Crossover Panel -->
        <Border Grid.Row="1" Background="{StaticResource BgSecondary}" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="60"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Crossover" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center" Foreground="{StaticResource TextSecondary}" Width="70"/>
                <Slider Grid.Column="1" x:Name="SldGlobalCrossover" Minimum="40" Maximum="200" Value="80" Style="{StaticResource ModernSlider}" VerticalAlignment="Center" ValueChanged="SldGlobalCrossover_ValueChanged" SmallChange="1" LargeChange="10" TickFrequency="1" IsSnapToTickEnabled="True"/>
                <TextBlock Grid.Column="2" x:Name="TxtGlobalCrossover" Text="{Binding ElementName=SldGlobalCrossover, Path=Value, StringFormat={}{0:0} Hz}" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center" TextAlignment="Right"/>
            </Grid>
        </Border>

        <!-- Control Panel (Refresh & Auto-Sync) -->
        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Available Outputs" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" Foreground="{StaticResource TextSecondary}"/>
            <Button Grid.Column="1" x:Name="BtnAutoSync" Content="Auto-Sync" Width="120" Height="30" Style="{StaticResource ActionButton}" Background="#312E81" Margin="0,0,10,0" Click="BtnAutoSync_Click"/>
            <Button Grid.Column="2" Content="Refresh" Width="80" Height="30" Style="{StaticResource ActionButton}" Background="#2A2A35" Click="BtnRefresh_Click"/>
        </Grid>

        <!-- Device List -->
        <Border Grid.Row="3" Background="{StaticResource BgSecondary}" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" CornerRadius="8" Padding="5">
            <ListBox x:Name="LstDevices" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.Resources>
                    <Style TargetType="ScrollBar" BasedOn="{StaticResource ModernScrollBar}"/>
                </ListBox.Resources>
                <!-- ListBoxItem Template -->
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Margin" Value="2"/>
                        <Setter Property="Padding" Value="10"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="BgBorder" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="true">
                                            <Setter TargetName="BgBorder" Property="Background" Value="#2A2A35"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="true">
                                            <Setter TargetName="BgBorder" Property="Background" Value="#312E81"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <!-- Item Layout -->
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/> <!-- Checkbox -->
                                <ColumnDefinition Width="Auto"/> <!-- Device Icon -->
                                <ColumnDefinition Width="*"/>    <!-- Details -->
                                <ColumnDefinition Width="Auto"/> <!-- Delay Control -->
                            </Grid.ColumnDefinitions>
                            <!-- CheckBox bound to IsSelected -->
                            <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected}" Style="{StaticResource ModernCheckBox}" Checked="CheckBox_Changed" Unchecked="CheckBox_Changed"/>
                            <!-- Icon path (Speaker shape) -->
                            <Path Grid.Column="1" Margin="12,0,12,0" Width="16" Height="16" Fill="{StaticResource TextSecondary}" Stretch="Uniform"
                                  Data="M3,9 v6 h4 l5,5 v-16 l-5,5 h-4 z M14.5,12 c0-1.77-1.02-3.29-2.5-4.03 v8.05 c1.48-.73 2.5-2.25 2.5-4.02 z M12,3.23 v2.06 c2.89,.86 5,3.54 5,6.71 s-2.11,5.85-5,6.71 v2.06 c4.01-.91 7-4.49 7-8.77 s-2.99-7.86-7-8.77 z"/>
                            <!-- Name and description -->
                            <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="0,0,10,0">
                                <TextBlock Text="{Binding Name}" FontSize="13" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{Binding StatusText}" FontSize="11" Foreground="{StaticResource TextSecondary}" Margin="0,2,0,0"/>
                            </StackPanel>
                            <!-- Controls Panel (Delay, Volume, Channel Isolation) -->
                            <StackPanel Grid.Column="3" Orientation="Vertical" VerticalAlignment="Center" Margin="10,0,5,0">
                                <!-- Delay Control -->
                                <Grid Margin="0,2" Visibility="{Binding ElementName=RootWindow, Path=DelaySlidersVisibility}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="70"/>
                                        <ColumnDefinition Width="45"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="Delay" FontSize="10" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Width="30"/>
                                    <Slider Grid.Column="1" Minimum="0" Maximum="1000" Value="{Binding DelayMs, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernSlider}" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="2" Text="{Binding DelayMs, StringFormat={}{0}ms}" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center" TextAlignment="Right"/>
                                </Grid>
                                <!-- Volume Control -->
                                <Grid Margin="0,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="70"/>
                                        <ColumnDefinition Width="45"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="Vol" FontSize="10" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Width="30"/>
                                    <Slider Grid.Column="1" Minimum="0" Maximum="100" Value="{Binding Volume, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernSlider}" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="2" Text="{Binding Volume, StringFormat={}{0:0}%}" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center" TextAlignment="Right"/>
                                </Grid>
                                <!-- Channel Isolation ComboBox -->
                                <Grid Margin="0,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="Chan" FontSize="10" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Width="30"/>
                                    <ComboBox Grid.Column="1" SelectedValue="{Binding IsolationMode, UpdateSourceTrigger=PropertyChanged}" SelectedValuePath="Tag" Style="{StaticResource ModernComboBox}" Width="120" Height="22" HorizontalAlignment="Right" SelectionChanged="IsolationComboBox_SelectionChanged">
                                        <ComboBoxItem Content="Stereo" Tag="{x:Static local:ChannelIsolationMode.Stereo}" Style="{StaticResource ModernComboBoxItem}"/>
                                        <ComboBoxItem Content="Left Only" Tag="{x:Static local:ChannelIsolationMode.LeftOnly}" Style="{StaticResource ModernComboBoxItem}"/>
                                        <ComboBoxItem Content="Right Only" Tag="{x:Static local:ChannelIsolationMode.RightOnly}" Style="{StaticResource ModernComboBoxItem}"/>
                                    </ComboBox>
                                </Grid>
                                <!-- Frequency Mode ComboBox -->
                                <Grid Margin="0,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="X-Over" FontSize="10" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Width="30"/>
                                    <ComboBox Grid.Column="1" SelectedValue="{Binding CrossoverMode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SelectedValuePath="Tag" Style="{StaticResource ModernComboBox}" Width="120" Height="22" HorizontalAlignment="Right" SelectionChanged="CrossoverComboBox_SelectionChanged">
                                        <ComboBoxItem Content="Full Range" Tag="{x:Static local:CrossoverMode.FullRange}" Style="{StaticResource ModernComboBoxItem}"/>
                                        <ComboBoxItem Content="Subwoofer (Low-Pass)" Tag="{x:Static local:CrossoverMode.LowPass}" Style="{StaticResource ModernComboBoxItem}"/>
                                        <ComboBoxItem Content="Mains (High-Pass)" Tag="{x:Static local:CrossoverMode.HighPass}" Style="{StaticResource ModernComboBoxItem}"/>
                                    </ComboBox>
                                </Grid>
                                <!-- Measured Latency readout -->
                                <TextBlock Text="{Binding MeasuredLatencyText}" FontSize="9" Foreground="{StaticResource TextSecondary}" HorizontalAlignment="Right" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- Start/Stop Action -->
        <StackPanel Grid.Row="4" Margin="0,20,0,10">
            <Button x:Name="BtnStartStop" Content="Start Routing" Style="{StaticResource ActionButton}" Background="{StaticResource SuccessColor}" Click="BtnStartStop_Click"/>
        </StackPanel>

        <!-- Activity/Status Meter -->
        <Border Grid.Row="5" Background="{StaticResource BgSecondary}" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,5,0,0">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="TxtCaptureDevice" Text="Capture: Idle" FontSize="12" Foreground="{StaticResource TextSecondary}"/>
                    <TextBlock Grid.Column="1" x:Name="TxtVolume" Text="0%" FontSize="12" Foreground="{StaticResource TextSecondary}" FontWeight="Bold"/>
                </Grid>
                <!-- Volume meter progress bar -->
                <ProgressBar x:Name="PrgVolume" Height="6" Background="#2A2A35" Foreground="{StaticResource SuccessColor}" BorderThickness="0" Margin="0,8,0,0" Maximum="1" Value="0">
                    <ProgressBar.Template>
                        <ControlTemplate TargetType="ProgressBar">
                            <Border Background="{TemplateBinding Background}" CornerRadius="3" SnapsToDevicePixels="true">
                                <Grid x:Name="TemplateRoot">
                                    <Border x:Name="PART_Track" CornerRadius="3" />
                                    <Border x:Name="PART_Indicator" HorizontalAlignment="Left" CornerRadius="3" Background="{TemplateBinding Foreground}"/>
                                </Grid>
                            </Border>
                        </ControlTemplate>
                    </ProgressBar.Template>
                </ProgressBar>
            </StackPanel>
        </Border>
    </Grid>
</Window>
